# Contributing（日本語）

## 技術的な制約
- npm を使う（yarn/pnpmは使わない）
- `npm run build` が通ることを常に優先
- 依存追加は原則しない（Issueで明示された場合のみ）

## コード方針
- 「退屈で分かりやすい」コードを優先
- コンポーネントを大きくしすぎない
- 過剰な抽象化をしない
- UIはミニマルでモダンに（装飾過多にしない）

## PRの観測点
- 変更理由・確認手順が残っているか
- スコープが守られているか
- 次の課題が明示されているか
